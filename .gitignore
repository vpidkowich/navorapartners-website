# Large video files (GitHub 100MB limit)
*.mp4
*.mp3

# Zip archives
*.zip

# Duplicate folders
videos/videos (2)/

# OS files
.DS_Store
Thumbs.db

# Local secrets (Cloudflare Pages convention) — never commit
.dev.vars
.dev.vars.*
.env
.env.local
